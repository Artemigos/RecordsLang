ns RecordsLang.Test;

use System.Data;

def +set +defaultConstructor +dataConstructor;

proto dto +serializable;
proto model -set +copyConstructor -defaultConstructor +valEquals;

model Model(int Field1, string Field2, long Field3) valEquals:Field1,Field2;
dto Dto(string Hai);
Default(int fdsa, DataRow src);
